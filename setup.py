from setuptools import setup, find_packages

setup(
    name='sparkify_star_schema_tasks',
    version='0.1.0',
    packages=find_packages(include=['sparkify_star_schema_tasks']),
    install_requires=[]
)